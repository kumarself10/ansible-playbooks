- hosts: all
  become: true
  tasks:
    - name: Create user
        user:
            name: mario
            shell: /bin/zsh
 
      - name: Install tree
        yum:
            name: tree
            state: latest
